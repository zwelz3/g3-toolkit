# Enforce pnpm via corepack (packageManager field in package.json).
# pnpm-specific defaults are fine without explicit config:
#   auto-install-peers = true (pnpm default since v8)
#   strict-peer-dependencies = false (pnpm default)
#   shamefully-hoist = false (pnpm default; strict isolation)
